import React from 'react'
import { Icon } from './Icon'

const MoreVertIcon: Icon = ({ alt, className }) => (
  <img className={className} alt={alt} src="/img/icon-more-vert.svg" />
)

export default MoreVertIcon
