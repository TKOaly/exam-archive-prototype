import React from 'react'
import { Icon } from '../Icon'

const FolderIcon: Icon = ({ alt, className }) => (
  <img className={className} alt={alt} src="/img/icon-folder.svg" />
)

export default FolderIcon
