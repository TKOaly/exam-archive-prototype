import React from 'react'
import { Icon } from './Icon'

const DeleteIcon: Icon = ({ alt, className }) => (
  <img className={className} alt={alt} src="/img/icon-delete.svg" />
)

export default DeleteIcon
