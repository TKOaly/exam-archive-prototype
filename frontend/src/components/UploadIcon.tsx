import React from 'react'
import { Icon } from './Icon'

const UploadIcon: Icon = ({ alt, className }) => (
  <img className={className} alt={alt} src="/img/icon-upload.svg" />
)

export default UploadIcon
